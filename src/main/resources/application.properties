spring.application.name=telaSanta

# ===========================
#   CONFIGURAÇÃO DO MYSQL
# ===========================
spring.datasource.url=jdbc:mysql://localhost:3306/cartoesc?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
spring.datasource.username=root
spring.datasource.password=Jubileu82828@

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===========================
#   JPA / HIBERNATE
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? Dialeto correto para Spring Boot 3 (Hibernate 6+)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===========================
#   SERVIDOR
# ===========================
server.port=8080
